# 변경 로그 (Changelog)

## 2026-05-15

### 성능/사용성
- `기간조회` 기본값 개선: 데이터의 맨 첫날이 아니라 **당월 1일 ~ 데이터 최신일**로 자동 시작.
- 공정밸런스 탭 계산 최적화:
  - 결과2(`매칭결과`) 기반 집계를 1회 사전 생성 후 기간 변경 시 **필터만 적용**하도록 변경(리런 속도 개선).
  - `공장별 요약/상세 테이블`은 토글로 기본 숨김 처리(표 렌더 비용 절감).

### 공정밸런스 UI
- `공장별 종합점수 추이` 선그래프를 접힘(expander) 형태로 추가(관별 종합점수 트렌드 확인).
- `공장별 요약` + `상세 테이블`을 엑셀 1개(시트 2개)로 일괄 다운로드 지원(수량 콤마 / % 소수 1자리 / 점수 소수 1자리).
- 다운로드 버튼은 “원클릭” 동작을 위해 미리 생성된 bytes를 재사용하도록 `session_state` 캐시 적용.

### 정리
- 개발 중 생성된 `streamlit_*.err/out.log` 파일 삭제 및 `.gitignore`에 추가.

## 2026-04-20

### UI/레이아웃
- 상단 결과파일 선택 UI(`selectbox`) 숨김: 최신 `유효생산량_결과*.xlsx` 자동 선택.
- KPI 영역 레이아웃 변경: `총 생산량` + `규격 대응률`을 1개의 큰 카드(내부 2셀)로 합치고, 우측에 `정확/초과/비정형` 카드 3개 배치.
- KPI 카드와 `지표 정의/상세 보기` 사이 간격(마진) 추가로 겹침 개선.

### 공장 비교(표/상세)
- `규격 대응률` 선택 시, 하단 상세 테이블을 SKU 기준으로 표시하도록 변경:
  - 공장/신규분류요약/총 생산 SKU/규격 대응 SKU/규격 대응률(%)
- `규격 대응률` 탭의 별도 요약표 제거(다른 탭과 동일하게 상세만 노출).
- `규격 대응률` 탭에서만 보이던 추가 제목(예: "공장-신규분류 SKU 기준 상세") 제거하여 탭 간 제목 스타일 일관화.
