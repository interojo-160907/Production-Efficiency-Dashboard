# 운영 가이드 (성능/누적 관리)

## 목표
- 날짜가 늘어도 느려지지 않게 운영(특히 Streamlit Community Cloud 메모리 제한 회피)
- 누적 관리(최근 N개월 갱신) 신뢰성 확보

## 권장 운영 방식(필수)
### 1) 전처리: Parquet store 생성/갱신
- 전처리 스크립트: `C:\Users\심민식\Documents\이상호\개인프로그램\APS 유효생산량 집계\aps_yield_dashboard.py`
- 실행 후 생성되는 경로: `outputs\store\`
- 구조: `outputs\store\<table>\YYYY-MM.parquet` (월 파티션)

전처리는 기본적으로 **최근 `REFRESH_MONTHS`개월만 재계산/덮어쓰기** 하며, store에는 해당 월 parquet가 갱신됩니다.

### 전처리 범위(당월/룩백)
전처리는 실행 시 범위를 정해서 그 범위에 걸친 월만 갱신합니다.
- 기본: `APS_MODE=month` (당월 1일 ~ 전일)
- 전월 백필: `APS_MODE=prev_month` (전월 1일 ~ 전월 말일)
- 연휴/지연 반영: `APS_MODE=lookback` + `LOOKBACK_DAYS=7` (전일부터 N일치)

PowerShell 예시(전월 백필):
- `$env:APS_MODE='prev_month'`
- `python aps_yield_dashboard.py`

PowerShell 예시(룩백 7일):
- `$env:APS_MODE='lookback'`
- `$env:LOOKBACK_DAYS='7'`
- `python aps_yield_dashboard.py`

특정 기간을 직접 생성(예: 전월만 만들기)하려면:
- `$env:RANGE_START='2026-04-01'`
- `$env:RANGE_END='2026-04-30'`
- `python aps_yield_dashboard.py`

## (선택) 전처리 후 대시보드 클론폴더로 자동 복사
전처리 실행이 끝나면 `outputs/store`를 대시보드 Git 클론 폴더로 자동 동기화할 수 있습니다.

- `$env:DASHBOARD_REPO_PATH='C:\path\to\dashboard-clone'`
- `python aps_yield_dashboard.py`

추가 옵션:
- 월별 엑셀도 같이 복사: `$env:SYNC_MONTHLY_EXCEL=1`
  - 참고: 대시보드 GitHub 리포는 용량/속도를 위해 `outputs/*.xlsx`를 `.gitignore`로 제외했습니다. (로컬 확인용으로만 사용)

### 2) 대시보드: Parquet(store)로 로드
- 대시보드: `streamlit_app.py`
- 사이드바에서 `Parquet(store) 사용(추천)`을 켭니다.
- `Parquet store 경로`에 전처리 폴더의 `outputs\store` 경로를 지정합니다.
  - 예: `C:\Users\심민식\Documents\이상호\개인프로그램\APS 유효생산량 집계\outputs\store`

store 모드에서는 기간 선택 후 **해당 월만 로드**하여 메모리 사용을 제한합니다.

## 엑셀(유효생산량_결과*.xlsx) 정책
### 월별 엑셀
- 공유/검증용으로만 사용 권장
- 전처리에서 `WRITE_MONTHLY_EXCEL=True`로 생성(기본 True)

### 매칭결과 시트(무거움)
- 월별 엑셀에서 `매칭결과` 시트는 기본 OFF입니다(대시보드는 store 기준으로 동작).
- 필요 시에만 환경변수로 켭니다.
  - `$env:WRITE_MATCHING_EXCEL=1`

### 상세 store(매칭결과 parquet, 매우 무거움)
- `outputs/store/result1_matching` 및 `outputs/store/result2_matching`은 기본 저장/복사하지 않습니다.
- 필요할 때만 환경변수로 켭니다.
  - `$env:WRITE_DETAIL_STORE=1`

공정 밸런스는 상세 매칭결과 없이도 동작하도록 `outputs/store/result2_proc_base`, `outputs/store/result2_det_base` 집계를 기본 저장합니다.

### 누적 엑셀(매우 무거움)
- 기본 OFF
- 필요 시에만 환경변수로 켭니다.
  - PowerShell(세션): `$env:WRITE_BASE_EXCEL=1`
  - 실행 후 원복: `$env:WRITE_BASE_EXCEL=0`

## Cloud에서 “리소스 제한 초과”가 뜰 때(응급)
- 사이드바 `상세(매칭결과) 로드`는 기본 OFF 유지(가장 큼)
- 기간을 먼저 좁힌 뒤 필요한 경우에만 상세 로드 ON
- 사이드바 `캐시 비우기` 버튼으로 캐시 정리 후 새로고침
