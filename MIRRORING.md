# 컨트롤타워 데이터 미러링

2026-09-17부터 `outputs/mirror/dashboard.sqlite`를 우선 조회합니다.
DB가 없는 초기 환경에서만 기존 Parquet 화면으로 진입합니다.

- 데이터: 컨트롤타워의 공장·공정·신규분류별 일별 집계와 점수 기준.
- 계산: `tower_mirror/dashboard_snapshot.py`는 설치용 컨트롤타워 3의 동일 파일입니다.
  DB 접근만 읽기 전용 어댑터로 대체합니다. 이 파일을 웹에서 독자 수정하지 않습니다.
- 날짜: 한국 시간, 게시 당시 전일까지의 실적. PC가 꺼져 있으면 마지막 게시본 유지.
- 과거 정정: 전체 게시 대상 집계를 비교하며 수정·삭제도 반영합니다.
- 보안: 운영 DB 전체, API 원문, 인증정보, 개인 설정은 게시하지 않습니다.
- 성능: 약 2.7MB 집계 DB, 날짜·공장 인덱스, 버전·기간 캐시(최대 4개).
  상세 표는 선택 시 렌더링하고 엑셀은 준비 버튼을 누를 때 생성합니다.
- 표시 시각: `publication.json`의 `pushed_at`은 데이터 Git push 성공 후 기록합니다.
  DB 생성시각이나 페이지 접속시각이 아닙니다. 변화 없는 재확인은 시각을 바꾸지 않습니다.
- 데이터와 계산 코드의 해시가 다르면 잘못된 계산 대신 오류를 표시합니다.

컨트롤타워 PC의 설정은 선택한 데이터 폴더의
`hub/config/streamlit_mirror.json`, 실행 기록은 `streamlit-mirror/status.json`입니다.
게시 작업은 `streamlit-mirror/checkout` 전용 Git 폴더를 사용합니다.
사용자가 작업 중인 GitHub 폴더를 자동으로 커밋하거나 덮어쓰지 않습니다.

게시 순서: 시작 수집 성공 → 모듈 준비 완료 → 대기 수집 처리 → 15초 유휴 확인 →
읽기 트랜잭션으로 게시 DB 생성 → 변경 확인 → 데이터 push → 성공시각 기록 push.
실패하면 이전 GitHub 게시본을 유지하며, 푸시 실패로 남은 커밋은 다음 실행에서 재시도합니다.

검증: 4~9월 6개월의 두 계산 함수 결과(전체 중첩 KPI·차트·표·점수)를
컨트롤타워 원본 함수와 비교한 12건 모두 동일. 과거 수정·삭제, 무변경,
실패 시 이전 게시본 보존, 게시 실패 재시도, 준비 상태 차단을 테스트했습니다.
